example:
  property: I AM DEV

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    url: jdbc:postgresql://localhost:5432/movie_db
    password: password
  jpa:
    hibernate:
      ddl-auto: update